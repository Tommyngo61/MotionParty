# Handoff

`Meter-Node.bundle` is this directory's parent, packaged as a standalone git
repository ready to push to `MeterHome/Meter-Node`. See [PUSH.md](PUSH.md).

Delete this directory once the repository exists — it is a duplicate of the
tree beside it, kept only because this session could not push to the MeterHome
org itself.
